// News posts cleared on request. Add entries back with this shape:
//   { slug, title, date, excerpt, url, body }
// where `body` is either null (renders link to source URL) or an array of
// typed blocks: { type: 'p' | 'h2' | 'h3' | 'ul', text/items: ... }.

export const NEWS_POSTS = [];
